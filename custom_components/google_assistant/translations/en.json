{
    "entity": {
        "button": {
            "sync_devices": {
                "name": "Synchronize devices"
            }
        }
    },
    "options": {
        "step": {
            "init": {
                "title": "Entities & Presence",
                "description": "Select which entities to expose to Google Assistant and set the global presence sensor. UI settings take priority over YAML when both are configured.",
                "data": {
                    "presence_entity": "Global presence entity",
                    "exposed_entities": "Entities exposed to Google Assistant"
                },
                "data_description": {
                    "presence_entity": "Entity that tracks whether you are home (ON = home, OFF = away). Used by entities with presence requirement enabled.",
                    "exposed_entities": "Select the entities that Google Assistant can see and control. Only domains supported by Google Assistant are shown."
                }
            },
            "security": {
                "title": "Security Settings",
                "description": "Configure security features for your {exposed_count} exposed entities.",
                "data": {
                    "require_acknowledgment": "Require voice acknowledgment",
                    "require_presence": "Require presence"
                },
                "data_description": {
                    "require_acknowledgment": "Select entities that require voice confirmation (\"Are you sure?\") before executing commands via Google Assistant.",
                    "require_presence": "Select entities that should only respond to commands when you are home (based on the presence entity set in the previous step)."
                }
            }
        }
    },
    "services": {
        "request_sync": {
            "description": "Sends a request_sync command to Google.",
            "fields": {
                "agent_user_id": {
                    "description": "Only needed for automations. Specific Home Assistant user ID (not username, ID in Settings > People > Users > under username) to sync with Google Assistant. Not needed when you use this action through Home Assistant frontend or API. Used in automation, script or other place where context.user_id is missing.",
                    "name": "Agent user ID"
                }
            },
            "name": "Request sync"
        },
        "export_unleashed_config": {
            "name": "Export unleashed config",
            "description": "Exports the current unleashed security settings (require_acknowledgment, require_presence, presence_entity) to a YAML file in your config directory. Useful for migrating back to YAML-only configuration.",
            "fields": {
                "filename": {
                    "name": "Filename",
                    "description": "Name of the export file, written to your Home Assistant config directory."
                }
            }
        }
    }
}
